export enum EnumAppEnv {
    local = 'localhost',
    dev = 'development',
    tst = 'tst',
    production = 'production',
}

export enum EnumHttpMethod {
    get = 'get',
    post = 'post',
    put = 'put',
    delete = 'delete',
}

export enum EnumAppMode {
    design = 'design',
    preview = 'preview',
    view = 'view',
    add = 'add',
    edit = 'edit',
}
