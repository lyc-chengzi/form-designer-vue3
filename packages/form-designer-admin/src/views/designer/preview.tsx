import { defineComponent } from 'vue';
import './preview.less';

export default defineComponent({
    name: 'designer-preview',
    render() {
        return (
            <div id="form-designer-preview" class="form-designer-preview">
                预览页面
            </div>
        );
    },
});
