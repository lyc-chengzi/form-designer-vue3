import { IConfig } from '../interface/config';

const localConfig: IConfig = {
    port: 3030,
    mongodb: {
        db: '',
        host: '',
        port: 3000,
    },
};
export default localConfig;
